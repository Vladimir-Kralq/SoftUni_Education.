function printSumFirstAndLast(arr) {

let sum=0;

let firstNum=Number(arr[0]);
let lastNum=Number(arr[arr.length-1]);

sum=firstNum +lastNum;
console.log(sum);









}
printSumFirstAndLast(['20', '30', '40']);